# Environment Variables per UrbanMind

# JWT Secret (usa una stringa casuale sicura in produzione)
JWT_SECRET=your-secret-key-change-in-production

# MongoDB URI (per sviluppo locale)
MONGODB_URI=mongodb://localhost:27017/urbanmind

# Backend Port
PORT=3000

# Node Environment
NODE_ENV=development
